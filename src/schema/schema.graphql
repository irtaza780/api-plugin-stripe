type Mutation {
  createStripeCustomer(
    email: String!
    name: String!
    description: String
  ): StripeCustomer!
}

type StripeCustomer {
  id: String!
  email: String!
  name: String!
  description: String
}
